import React from 'react';
import { DialogTitleProps } from '@mui/material/DialogTitle';
declare function PopperTitle({ ...props }: DialogTitleProps): React.JSX.Element;
declare namespace PopperTitle {
    var defaultProps: {
        id: string;
    };
}
export * from '@mui/material/DialogTitle';
export default PopperTitle;
